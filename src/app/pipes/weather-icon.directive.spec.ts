import { WeatherIconDirective } from './weather-icon.pipe';

describe('WeatherIconDirective', () => {
  it('should create an instance', () => {
    const directive = new WeatherIconDirective();
    expect(directive).toBeTruthy();
  });
});
